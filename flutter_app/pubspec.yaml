name: think_twice_flutter
description: ThinkTwice mobile-first Flutter rebuild.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  fl_chart: ^1.0.0
  google_maps_flutter: ^2.12.1
  geolocator: ^13.0.2
  http: ^1.2.2
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.3
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
